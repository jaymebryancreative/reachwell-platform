import { useState } from 'react'
import { Bell, CalendarDays, ChevronDown, FolderKanban, Home, Menu, MessageCircle, ShieldCheck, Users, X, Activity, History, HeartPulse, BookOpen, DollarSign } from 'lucide-react'
import { MissionMode } from '../features/mission/MissionMode'
import { ProjectsWorkspace } from '../features/projects/ProjectsWorkspace'
import { TeamsWorkspace } from '../features/teams/TeamsWorkspace'
import { PeopleWorkspace } from '../features/people/PeopleWorkspace'
import { EventsWorkspace } from '../features/events/EventsWorkspace'
import { SignInWorkspace } from '../features/signin/SignInWorkspace'
import { FollowUpsWorkspace } from '../features/followups/FollowUpsWorkspace'
import { TeamProgressWorkspace } from '../features/progress/TeamProgressWorkspace'
import { ActivityWorkspace } from '../features/activity/ActivityWorkspace'
import { OperationsHub } from '../features/operations/OperationsHub'
import { useReachWellContext } from '../lib/reachwellContext'
import './app.css'
import '../features/events/events.css'
import '../features/signin/signin.css'
import '../features/progress/progress.css'
import '../features/activity/activity.css'
import '../features/operations/operations.css'

type View = 'home'|'people'|'mission'|'projects'|'teams'|'events'|'signin'|'followups'|'progress'|'activity'|'relationships'|'communication'|'resources'|'finance'|'admin'
const navigation:{id:View;label:string;icon:typeof Home}[]=[{id:'home',label:'Overview',icon:Home},{id:'people',label:'People',icon:Users},{id:'teams',label:'Teams',icon:Users},{id:'projects',label:'Projects',icon:FolderKanban},{id:'events',label:'Events',icon:CalendarDays},{id:'signin',label:'Sign-In Mode',icon:Users},{id:'mission',label:'Mission Mode',icon:ShieldCheck},{id:'relationships',label:'Relationship Health',icon:HeartPulse},{id:'followups',label:'Follow-Ups',icon:CalendarDays},{id:'progress',label:'Team Progress',icon:Activity},{id:'activity',label:'History',icon:History},{id:'communication',label:'Communication',icon:MessageCircle},{id:'resources',label:'Resources',icon:BookOpen},{id:'finance',label:'Giving & Finance',icon:DollarSign},{id:'admin',label:'Administration',icon:ShieldCheck}]
export function AppShell(){const{user,organizationName,organizationRole,loading:contextLoading, error:contextError}=useReachWellContext();const[view,setView]=useState<View>('home');const[mobileOpen,setMobileOpen]=useState(false);const[signInEventId,setSignInEventId]=useState<string|undefined>();const selectView=(next:View)=>{setView(next);setMobileOpen(false)};const openSignIn=(eventId:string)=>{setSignInEventId(eventId);selectView('signin')};const displayName=user?.user_metadata?.full_name||user?.user_metadata?.name||user?.email?.split('@')[0]||'ReachWell member';const initials=displayName.split(/\s+/).map((part:string)=>part[0]).join('').slice(0,2).toUpperCase();return <div className="rw-app-shell"><aside className={`rw-sidebar ${mobileOpen?'is-open':''}`} aria-label="Primary navigation"><div className="rw-brand"><span className="rw-brand-mark">R</span><span>reachwell</span></div><nav>{navigation.map(({id,label,icon:Icon})=><button key={id} className={`rw-nav-item ${view===id?'is-active':''}`} onClick={()=>selectView(id)}><Icon size={19} strokeWidth={2.1}/><span>{label}</span></button>)}</nav><div className="rw-sidebar-footer">Built for people who show up.</div></aside>{mobileOpen&&<button className="rw-scrim" aria-label="Close navigation" onClick={()=>setMobileOpen(false)}/>}<main className="rw-main"><header className="rw-topbar"><button className="rw-icon-button rw-mobile-menu" onClick={()=>setMobileOpen(v=>!v)} aria-label="Toggle navigation">{mobileOpen?<X size={21}/>:<Menu size={21}/>}</button><div className="rw-context"><span className="rw-eyebrow">{organizationName||(contextLoading?'Connecting workspace':'Organization workspace')}</span><strong>{navigation.find(n=>n.id===view)?.label}</strong></div><div className="rw-topbar-actions"><button className="rw-icon-button" aria-label="Notifications"><Bell size={19}/></button><button className="rw-user-menu"><span className="rw-avatar">{initials||'RW'}</span><span className="rw-user-copy"><strong>{displayName}</strong><small>{organizationRole||(contextError?'Organization access required':'ReachWell member')}</small></span><ChevronDown size={16}/></button></div></header>{contextError&&<div className="rw-context-alert" role="status">{contextError}</div>}<section className="rw-content">{view==='home'&&<Overview onNavigate={selectView}/>} {view==='people'&&<PeopleWorkspace/>}{view==='mission'&&<MissionMode/>}{view==='projects'&&<ProjectsWorkspace/>}{view==='teams'&&<TeamsWorkspace/>}{view==='events'&&<EventsWorkspace onOpenSignIn={openSignIn}/>} {view==='signin'&&<SignInWorkspace eventId={signInEventId} onBack={()=>selectView('events')}/>} {view==='relationships'&&<OperationsHub section="relationships"/>}{view==='followups'&&<FollowUpsWorkspace/>}{view==='progress'&&<TeamProgressWorkspace/>}{view==='activity'&&<ActivityWorkspace/>}{view==='communication'&&<OperationsHub section="communication"/>}{view==='resources'&&<OperationsHub section="resources"/>}{view==='finance'&&<OperationsHub section="finance"/>}{view==='admin'&&<OperationsHub section="admin"/>}</section></main></div>}
function Overview({onNavigate}:{onNavigate:(view:View)=>void}){return <div className="rw-overview"><div className="rw-hero"><div><span className="rw-eyebrow">Ready to serve</span><h1>One place to organize the work that matters.</h1><p>ReachWell brings people, teams, projects, events, communication, and field operations into one connected workspace.</p></div><button className="rw-primary-button" onClick={()=>onNavigate('mission')}>Enter Mission Mode</button></div><div className="rw-overview-grid"><button className="rw-feature-card" onClick={()=>onNavigate('people')}><Users size={22}/><strong>Know your people</strong><span>Build connected profiles, organize relationships, and see where people serve.</span></button><button className="rw-feature-card" onClick={()=>onNavigate('relationships')}><HeartPulse size={22}/><strong>Relationship Health</strong><span>See needs, prayer, and follow-up signals that deserve attention.</span></button><button className="rw-feature-card" onClick={()=>onNavigate('events')}><CalendarDays size={22}/><strong>Run real events</strong><span>Turn an event into the context for attendance, teams, assignments, and field work.</span></button><button className="rw-feature-card" onClick={()=>onNavigate('followups')}><CalendarDays size={22}/><strong>Never lose the next step</strong><span>Turn conversations and visits into clear, trackable follow-ups.</span></button><button className="rw-feature-card" onClick={()=>onNavigate('activity')}><History size={22}/><strong>Know what happened</strong><span>Review the durable activity trail behind field assignments.</span></button><button className="rw-feature-card" onClick={()=>onNavigate('finance')}><DollarSign size={22}/><strong>Protect the money trail</strong><span>Review financial activity with status, amount, and accountability.</span></button><button className="rw-feature-card" onClick={()=>onNavigate('communication')}><MessageCircle size={22}/><strong>Stay connected</strong><span>Keep team conversations close to the work they support.</span></button><button className="rw-feature-card" onClick={()=>onNavigate('resources')}><BookOpen size={22}/><strong>Find resources</strong><span>Give volunteers and leaders one searchable home for knowledge.</span></button></div></div>}
