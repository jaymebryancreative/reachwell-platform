import { useEffect, useMemo, useState } from 'react'
import { ArrowDownRight, ArrowUpRight, DollarSign, RefreshCw, TrendingDown, TrendingUp } from 'lucide-react'
import { useReachWellContext } from '../../lib/reachwellContext'
import { supabase } from '../../lib/supabaseClient'

type Row={transaction_type:string;amount:number|string;transaction_date:string;status:string}
export function FinanceReportingWorkspace(){const{organizationId}=useReachWellContext();const[rows,setRows]=useState<Row[]>([]);const[loading,setLoading]=useState(false);const[error,setError]=useState('');const load=async()=>{if(!organizationId)return;setLoading(true);setError('');try{const{data,error}=await supabase.from('financial_transactions').select('transaction_type,amount,transaction_date,status').eq('organization_id',organizationId).gte('transaction_date',new Date(Date.now()-365*86400000).toISOString().slice(0,10)).order('transaction_date',{ascending:true});if(error)throw error;setRows((data??[]) as Row[])}catch(err){setError(err instanceof Error?err.message:'Unable to load finance reporting.')}finally{setLoading(false)}};useEffect(()=>{void load()},[organizationId]);const months=useMemo(()=>{const map=new Map<string,{income:number;expense:number}>();rows.forEach(r=>{const key=r.transaction_date.slice(0,7);const v=map.get(key)??{income:0,expense:0};if(r.transaction_type==='income')v.income+=Number(r.amount||0);if(r.transaction_type==='expense')v.expense+=Number(r.amount||0);map.set(key,v)});return [...map.entries()].slice(-12)},[rows]);const total=rows.reduce((a,r)=>a+(r.transaction_type==='income'?Number(r.amount||0):-Number(r.amount||0)),0);const income=rows.filter(r=>r.transaction_type==='income').reduce((a,r)=>a+Number(r.amount||0),0);const expense=rows.filter(r=>r.transaction_type==='expense').reduce((a,r)=>a+Number(r.amount||0),0);const recent=months.slice(-2);const trend=recent.length===2?(recent[1][1].income-recent[1][1].expense)-(recent[0][1].income-recent[0][1].expense):0;return <div className="finance-reporting"><header className="money-trail-heading"><div><span className="rw-eyebrow">GIVING & FINANCE</span><h1>Finance reporting</h1><p>See the movement behind the money trail across the last twelve months.</p></div><button className="rw-secondary-button" onClick={()=>void load()} disabled={loading}><RefreshCw size={16}/> {loading?'Refreshing…':'Refresh'}</button></header>{error&&<div className="rw-context-alert" role="alert">{error}</div>}<div className="money-trail-summary"><div><span>Income / giving</span><strong>${income.toLocaleString()}</strong><small><ArrowUpRight size={14}/> twelve-month inflow</small></div><div><span>Expenses</span><strong>${expense.toLocaleString()}</strong><small><ArrowDownRight size={14}/> twelve-month outflow</small></div><div><span>Net movement</span><strong>${total.toLocaleString()}</strong><small>{trend>=0?<TrendingUp size={14}/>:<TrendingDown size={14}/>} {trend>=0?'Improving':'Declining'} recent movement</small></div><div><span>Records</span><strong>{rows.length}</strong><small>financial records analyzed</small></div></div><section className="finance-report-card"><div className="finance-report-heading"><div><span className="rw-eyebrow">MONTHLY MOVEMENT</span><h2>Income vs. expenses</h2></div><DollarSign size={21}/></div>{loading?<div className="rw-empty-state">Loading finance reporting…</div>:!months.length?<div className="rw-empty-state">No financial records in the last twelve months.</div>:<div className="finance-months">{months.map(([month,v])=>{const max=Math.max(v.income,v.expense,1);return <div className="finance-month" key={month}><div className="finance-bars"><span style={{height:`${Math.max(4,v.income/max*100)}%`}} title={`Income ${v.income}`}/><span style={{height:`${Math.max(4,v.expense/max*100)}%`}} title={`Expenses ${v.expense}`}/></div><small>{month.slice(5)}</small></div>})}</div>}<div className="finance-legend"><span>Income</span><span>Expenses</span></div></section></div>}
